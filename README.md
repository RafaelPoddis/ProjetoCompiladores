# Projeto Final Compiladores

Este é um repositório para o projeto final da disciplina de compiladores da UNIFESP.

---

## Estrutura

Na pasta `src/` devem existir:

```
scanner.l        -> arquivo do Flex
parser.y         -> arquivo do Bison
main.c           -> arquivo contendo a função main()
```